Configuracoes JavaScript

Terminal > new terminal

Mudar para Git BAsh

Digitar no terminal: npm init -y


se digitar node -v mostra a versao do node

para instalar playwright digitar npm install -D @playwright/test

versao playwright digitar npx playwright --version

para inicializar o playwright digitar:  

	npm install --save-dev playwright

	npx playwright install


executar o teste digitar npx playwright test

executar abrindo janela digitar npx playwright test --headed

digitar npx playwright codegen https://www.saucedemo.com  para gravar tela com playwright
 
digitar npx playwright test comprar_mochila.spec.js para rodar um teste so


-------------------------------------------------------------

Modos de execução
- Headless (padrão): sem exibição
--> Não tem interferência do usuário
--> Leve
- Headed: com exibição

Melhores localizadores (2025):
- data-test (W3C)
- id
- name
- cssSelector
- class ou tag
- xPath

3 Fatores comuns de erro em automação:
- Seletor ruim
- Falta de Sincronismo
- "Programação Exótica"

Agenda
- Até a aula 18: Playwright
- 19 a 24: Appium
- 25 a 28: Postman
- 29 a 32: REST-assured
Termina: 19/novembro

Tem previsão para adicionar os testes em CI/CD
- Precisaria ensinar um pouco sobre Infra / Nuvem
- Na maioria das empresas, não é o QA que faz --> DevOps

Abordagens de automação
- Capture and Playback / Record and Play --> Selenium IDE / Playwright Codegen
- Script Simples - tudo junto em um só arquivo
- Script Estruturado --> organiza o que se usa em 2 ou + locais em um 3º local 

Credencias do GitHub para o Git
git config --global user.name "Iterasys"
git config --global user.email "contato@iterasys.com"

Decisão de imitar a navegação do usuário ou otimizar a velocidade

// imitou o usuário
  await page.locator('[data-test="username"]').click();
  await page.locator('[data-test="username"]').fill('standard_user');

// otimizou a execução
  await page.locator('[data-test="username"]').fill('standard_user');

E quando tem mais de um jeito de interagir
- clicar ou dar tab (login do Saucedemo)
- clicar na lupa ou dar Enter (Google)

Comandos para teclado:
<Tab>: await page.press('[data-test="username"]', 'Tab')
<Enter>: await page.locator(name="q", 'Enter')

Sortear ações? Pode ser uma alternativa a fazer só 1 
const usarClique = Math.random() < 0.7; // 70% das vezes clique e 30% Tab

Arquivo de configuração do Playwright: playwright.config.js
